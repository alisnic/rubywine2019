# My RubyWine 2019 talk

This repo contains materials from my RubyWine 2019 talk - "Creating a simple programming language in Ruby".

## Running the code

1. Install deps (`bundle install`)
2. Play with `code.rb`
3. Run it (`bundle exec ruby code.rb`)
